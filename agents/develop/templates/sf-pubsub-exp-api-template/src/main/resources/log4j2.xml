<?xml version="1.0" encoding="utf-8"?>
<Configuration status="INFO" name="cloudhub"
    packages="com.insulet.datadog,org.apache.logging.log4j">

    <Appenders>
        <RollingFile name="file"
            fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}${project.artifactId}.log"
            filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}${project.artifactId}-%i.log">
            <PatternLayout pattern="%-5p %d [%t] [processor: %X{processorPath}; event: %X{correlationId}] %c: %m%n"/>
            <SizeBasedTriggeringPolicy size="10 MB"/>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
        <DataDogLog4jAppender name="DATADOG"
            endpoint="${sys:DATADOG_HOST}"
            apiKey="${sys:DATADOG_API_KEY}"
            source="MuleSoft"
            host="${hostName}"
            service="${project.artifactId}"
            apiLayer="${sys:anypoint.platform.visualizer.layer}"
            environment="${sys:mule.env}"
            ddTags="integration:MuleSoft-To-DataDog"
            batchSize="500"
            maxBytesPerBatch="5242880"
            flushIntervalMs="5000"
            connectTimeoutMs="2000"
            readTimeoutMs="10000"
            retryEnabled="true"
            retryCount="3"
            enableCircuitBreaker="true"
            circuitBreakerFailures="5"
            circuitBreakerOpenMs="30000"
            compressPayloads="true">
            <PatternLayout pattern="%-5p %d [%t] [processor: %X{processorPath}; event: %X{correlationId}] %c: %m%n" />
        </DataDogLog4jAppender>
    </Appenders>

    <Loggers>
        <!-- Suppress noisy HTTP wire traffic (enable to DEBUG for local troubleshooting) -->
        <AsyncLogger name="org.mule.service.http" level="WARN"/>
        <AsyncLogger name="org.mule.extension.http" level="WARN"/>

        <!-- Built-in Mule logger component (not used — custom logger connector used instead) -->
        <AsyncLogger name="org.mule.runtime.core.internal.processor.LoggerMessageProcessor" level="INFO"/>

        <!--
            Insulet Custom Logger Connector categories (three required):
              com.insulet.general          — INFO-level logs without payload
              com.insulet.maskedpayload   — DEBUG-level logs with masked payload
              com.insulet.encryptedpayload — DEBUG-level logs with encrypted payload

            PROD: Set all three to WARN.
            Non-PROD: Set to INFO (general) and INFO (masked/encrypted) per environment needs.
        -->
        <AsyncLogger name="com.insulet.general" level="INFO" />
        <AsyncLogger name="com.insulet.maskedpayload" level="INFO" />
        <AsyncLogger name="com.insulet.encryptedpayload" level="INFO" />

        <AsyncRoot level="INFO">
            <AppenderRef ref="file"/>
            <AppenderRef ref="DATADOG"/>
        </AsyncRoot>
    </Loggers>

</Configuration>
